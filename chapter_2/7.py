# When a variable is used before it has been assigned a value, a NameError
# occurs. In the Python shell, write an expression that results in a NameError.

# bar is not defined and will return a NameError
print('foo:', bar)