# Following the function design recipe, define a function that has one
# parameter, a number, and returns that number tripled.

def tripler(number):
  """ (int) -> int

  Return the passed argument integer tripled in value.

  >>> tripler(3)
  9
  >>> tripler(4)
  12
  >>> tripler(-5)
  -15
  """
  return number * 3