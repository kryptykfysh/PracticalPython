# The variables side1, side2, and side3 refer to 3, 4, and 5, respectively. Using
# the format string method and those three variables, write an expression
# that produces 'The sides have lengths 3, 4, and 5.'

side1 = 3
side2 = 4
side3 = 5

print("The sides have lengths {}, {}, and {}.".format(side1, side2, side3))