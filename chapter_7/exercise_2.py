# Using the string method count, write an expression that produces the
# number of o's in 'tomato'.

test_string = 'tomato'
test_case = 'o'

print("{0} has {1} '{2}'s".format(test_string, test_string.count(test_case), test_case))